server:
  port: 80
  servlet:
    context-path: /api
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/todo_db
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
