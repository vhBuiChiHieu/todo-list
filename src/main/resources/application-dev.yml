server:
  port: 8081
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/todo_db
  jpa:
    hibernate:
      ddl-auto: update
