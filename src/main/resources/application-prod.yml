server:
  port: 8082
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/todo_db
  jpa:
    hibernate:
      ddl-auto: none
